<form method="post" action="{{ path('pag_inicio_delete', {'id': pag_inicio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pag_inicio.id) }}">
    <button class="btn">Delete</button>
</form>
