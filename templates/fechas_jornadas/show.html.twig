<!DOCTYPE html>

<title>FechasJornadas</title>

{% block body %}
    <h1>FechasJornadas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fechas_jornada.id }}</td>
            </tr>
            <tr>
                <th>Valencia</th>
                <td>{{ fechas_jornada.valencia }}</td>
            </tr>
            <tr>
                <th>Alicante</th>
                <td>{{ fechas_jornada.alicante }}</td>
            </tr>
            <tr>
                <th>Castellon</th>
                <td>{{ fechas_jornada.castellon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fechas_jornadas_index') }}">back to list</a>

    <a href="{{ path('fechas_jornadas_edit', {'id': fechas_jornada.id}) }}">edit</a>

    {{ include('fechas_jornadas/_delete_form.html.twig') }}
{% endblock %}
