<form method="post" action="{{ path('fechas_jornadas_delete', {'id': fechas_jornada.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fechas_jornada.id) }}">
    <button class="btn">Delete</button>
</form>
