{% extends "adminpage/layout.html.twig" %}

    {% block div %}
    <title>Entidades index</title>
    
        <h1 style="text-align:center">Entidades</h1><br>

    <table class="table table-striped table-hover" align="center" style="text-align: center;">
        <thead>
            <tr>
                {# <th>Id</th> #}
                <th>Logo</th>
                <th>Nombre</th>
                {# <th>Telefono</th>
                <th>Correo</th> #}
                {# <th>Localizacion</th>
                <th>Descripcion</th> #}
                {# <th>actions</th> #}
            </tr>
        </thead>
        <tbody>
        {% for pag_entidade in pag_entidades %}
            <tr>
                {# <td>{{ entidade.id }}</td> #}
                <td><img src=""></td>
                <td>{{ entidade.nombre }}</td>
                {# <td>{{ entidade.telefono }}</td>
                <td>{{ entidade.correo }}</td>
                <td>{{ entidade.localizacion }}</td>
                <td>{{ entidade.descripcion }}</td> #}
                <td>
                    <a href="{{ path('entidades_show', {'id': entidade.id}) }}">show</a>
                    <a href="{{ path('entidades_edit', {'id': entidade.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('pag_entidades_new') }}">Create new</a>
{% endblock %}