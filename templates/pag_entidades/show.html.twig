<!DOCTYPE html>

<title>PagEntidades</title>

{% block body %}
    <h1>PagEntidades</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ pag_entidade.id }}</td>
            </tr> #}
            {# <tr>
                <th>TitCabecera</th>
                <td>{{ pag_entidade.titCabecera }}</td>
            </tr> #}
            {# <tr>
                <th>Comunidad</th>
                <td>{{ pag_entidade.comunidad }}</td>
            </tr> #}
            <tr>
                {# <th>Tipo</th>
                <td>{{ pag_entidade.tipo }}</td>
            </tr> #}
            <tr>
                <th>Nombre</th>
                <td>{{ pag_entidade.nombre }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ pag_entidade.logo }}</td>
            </tr>
            {# <tr>
                <th>LocalizacionMaps</th>
                <td>{{ pag_entidade.localizacionMaps }}</td>
            </tr>
            <tr>
                <th>Correo</th>
                <td>{{ pag_entidade.correo }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ pag_entidade.telefono }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ pag_entidade.descripcion }}</td>
            </tr> #}
        </tbody>
    </table>

    <a href="{{ path('pag_entidades_index') }}">back to list</a>

    <a href="{{ path('pag_entidades_edit', {'id': pag_entidade.id}) }}">edit</a>

    {{ include('pag_entidades/_delete_form.html.twig') }}
{% endblock %}
