{% extends "adminpage/layout.html.twig" %}

    {% block div %}
    <title>PeliculasMirant</title>
     <h1>PeliculasMirant</h1>

    <table class="table" style="text-align:center">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ peliculas_mirant.id }}</td>
            </tr> #}
            <tr>
                <td><p style="font-weight:bold">Img</p><img src="/img/Carteles_mirant/{{ peliculas_mirant.img }} "width="600"></a></td>
            </tr>
            <tr>
                
                <td><p style="font-weight:bold">Titulo</p>{{ peliculas_mirant.titulo }}</td>
            </tr>
            <tr>
                
                <td><p style="font-weight:bold">Trailer</p><a href="{{ peliculas_mirant.trailer }}" style="color:blue;">{{ peliculas_mirant.trailer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('peliculas_mirant_index') }}">back to list</a>

    <a href="{{ path('peliculas_mirant_edit', {'id': peliculas_mirant.id}) }}">edit</a>

    {{ include('peliculas_mirant/_delete_form.html.twig') }}
{% endblock %}