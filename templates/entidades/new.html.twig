<!DOCTYPE html>

<title>New Entidades</title>

{% block body %}
    <h1>Create new Entidades</h1>

    {{ include('entidades/_form.html.twig') }}

    <a href="{{ path('entidades_index') }}">back to list</a>
{% endblock %}
